- document syntax
- replace Cat() with concatenation operator?
- add support for vector/matrix reallocation?
- add support for mpfr/biginteger/bigrational/bigdecimal arithmetics?
- get rid of `libls/'.
